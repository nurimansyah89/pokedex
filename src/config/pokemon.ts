export default {
  // API URL Endpoints
  apiUrl: process.env.REACT_APP_POKEMON_API_URL,

  // Pokemon Image URL Endpoint
  imageUrl: process.env.REACT_APP_POKEMON_IMAGE_URL,

  // We assign manually because the api isn't completed yet
  totalPokemons: process.env.REACT_APP_POKEMON_TOTAL,
};
